# 📋 2026-03-01 Todo List - 自定义类型参数支持

## 📌 需求概述
实现工具参数支持自定义类型，允许开发者定义复杂的嵌套数据结构作为工具参数，并在协议层正确描述、验证和解析。

---

## 任务1：创建开发分支 ⏱️ 5分钟

**需求描述：**
创建独立的开发分支进行功能开发

**功能点：**
- [ ] 创建新分支 `feature_20260301`
- [ ] 切换到新分支

**验证：**
```bash
git branch  # 确认当前在 feature_20260301 分支
```

---

## 任务2：扩展 McpArgumentAttribute 支持字段 ⏱️ 20分钟

**需求描述：**
当前 `McpArgumentAttribute` 仅支持 `AttributeTargets.Parameter`，需要扩展为支持类字段，用于描述自定义类型内部字段。

**功能点：**
- [ ] 修改 `AttributeUsage` 添加 `AttributeTargets.Field`
- [ ] 保持向后兼容性

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Server/Tools/Attributes.cs` (行54-67)

**实现代码：**
```csharp
[AttributeUsage(AttributeTargets.Parameter | AttributeTargets.Field)]
public class McpArgumentAttribute : Attribute
{
    public string Name { get; set; }
    public string Description { get; set; }
    public bool Required { get; set; }
}
```

**验证：**
- 代码编译通过
- 现有工具不受影响

---

## 任务3：实现自定义类型检测与验证 ⏱️ 1小时

**需求描述：**
检测参数类型是否为自定义类型，并验证自定义类型是否符合约束要求。

**功能点：**
- [ ] 在 `McpServer.cs` 中添加 `IsCustomType()` 方法
- [ ] 实现 `ValidateCustomType()` 递归验证方法
- [ ] 验证规则：
  - 必须至少有一个符合约束的字段
  - 字段必须同时标注 `[JsonProperty]` 和 `[McpArgument]`
  - 嵌套的自定义类型必须递归验证
  - 检测循环引用防止无限递归

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Server/McpServer.cs` (新增方法)

**实现要点：**
```csharp
/// <summary>
/// 检测类型是否为自定义类型（需要特殊处理的对象类型）
/// </summary>
private bool IsCustomType(Type type)
{
    if (type == null) return false;
    if (type.IsPrimitive) return false;
    if (type == typeof(string)) return false;
    if (type == typeof(decimal)) return false;
    if (type == typeof(DateTime)) return false;
    if (type == typeof(Guid)) return false;
    if (type.IsArray) return false;
    if (typeof(System.Collections.IEnumerable).IsAssignableFrom(type) && type != typeof(string)) return false;
    if (type.IsEnum) return false;
    if (type == typeof(object)) return false;
    
    // 排除Unity内置类型
    if (type.Namespace != null && type.Namespace.StartsWith("UnityEngine"))
        return false;
    
    // 排除System和mscorlib类型
    if (type.Namespace != null && (type.Namespace == "System" || type.Namespace.StartsWith("System.")))
        return false;
    
    return type.IsClass || type.IsValueType;
}

/// <summary>
/// 验证自定义类型是否符合约束
/// </summary>
private (bool isValid, string error) ValidateCustomType(Type type, HashSet<Type> visited = null)
{
    if (visited == null) visited = new HashSet<Type>();
    
    // 防止循环引用
    if (!visited.Add(type))
        return (true, null);  // 已验证过的类型，直接返回合法
    
    if (!IsCustomType(type))
        return (true, null);  // 不是自定义类型，无需验证
    
    var fields = type.GetFields(BindingFlags.Public | BindingFlags.Instance);
    var validFields = new List<FieldInfo>();
    
    foreach (var field in fields)
    {
        var jsonAttr = field.GetCustomAttribute<JsonPropertyAttribute>();
        var mcpAttr = field.GetCustomAttribute<McpArgumentAttribute>();
        
        // 字段必须同时有 JsonProperty 和 McpArgument
        if (jsonAttr == null || mcpAttr == null)
            continue;
        
        validFields.Add(field);
        
        // 递归验证嵌套的自定义类型
        if (IsCustomType(field.FieldType) || IsCustomTypeArray(field.FieldType))
        {
            var fieldType = field.FieldType;
            if (fieldType.IsArray)
                fieldType = fieldType.GetElementType();
            else if (fieldType.IsGenericType)
                fieldType = fieldType.GetGenericArguments()[0];
            
            var (nestedValid, nestedError) = ValidateCustomType(fieldType, visited);
            if (!nestedValid)
                return (false, $"Field '{field.Name}': {nestedError}");
        }
    }
    
    if (validFields.Count == 0)
        return (false, "No valid fields with [JsonProperty] and [McpArgument] attributes found");
    
    return (true, null);
}
```

**验证：**
- 合法的自定义类型通过验证
- 非法的自定义类型被正确拒绝
- 嵌套类型递归验证正常
- 循环引用不会导致无限递归

---

## 任务4：实现自定义类型 Schema 生成 ⏱️ 1.5小时

**需求描述：**
为自定义类型生成 JSON Schema，以嵌套object格式暴露，支持嵌套自定义类型和数组。

**功能点：**
- [ ] 在 `McpServer.cs` 的 `GenerateInputSchema()` 中添加自定义类型分支
- [ ] 实现 `GenerateCustomTypeSchema()` 方法（嵌套object格式）
- [ ] 实现 `AddCustomTypeProperties()` 方法
- [ ] 支持 `CustomType[]` 和 `List<CustomType>` Schema生成

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Server/McpServer.cs` (行504-584)

**Schema 格式示例：**
```json
{
  "type": "object",
  "description": "人员信息",
  "properties": {
    "name": {
      "type": "string",
      "description": "姓名"
    },
    "address": {
      "type": "object",
      "description": "地址信息",
      "properties": {
        "city": {
          "type": "string",
          "description": "城市"
        }
      },
      "required": ["city"]
    }
  },
  "required": ["name"]
}
```

**实现要点：**
```csharp
private JObject GenerateCustomTypeSchema(Type type, string description, HashSet<Type> visited = null)
{
    if (visited == null) visited = new HashSet<Type>();
    
    var schema = new JObject
    {
        ["type"] = "object",
        ["properties"] = new JObject(),
        ["required"] = new JArray()
    };
    
    if (!string.IsNullOrEmpty(description))
        schema["description"] = description;
    
    if (!visited.Add(type))
    {
        // 循环引用，返回基本schema
        return schema;
    }
    
    var properties = (JObject)schema["properties"];
    var required = (JArray)schema["required"];
    
    var fields = type.GetFields(BindingFlags.Public | BindingFlags.Instance);
    foreach (var field in fields)
    {
        var jsonAttr = field.GetCustomAttribute<JsonPropertyAttribute>();
        var mcpAttr = field.GetCustomAttribute<McpArgumentAttribute>();
        
        if (jsonAttr == null || mcpAttr == null)
            continue;
        
        string fieldName = jsonAttr.PropertyName ?? field.Name;
        string fieldDesc = mcpAttr.Description ?? "";
        bool isRequired = mcpAttr.Required;
        
        JObject fieldSchema;
        
        // 处理嵌套的自定义类型
        if (IsCustomType(field.FieldType))
        {
            fieldSchema = GenerateCustomTypeSchema(field.FieldType, fieldDesc, visited);
        }
        // 处理自定义类型数组
        else if (IsCustomTypeArray(field.FieldType))
        {
            var itemType = GetCustomArrayElementType(field.FieldType);
            var itemSchema = GenerateCustomTypeSchema(itemType, "", visited);
            
            fieldSchema = new JObject
            {
                ["type"] = "array",
                ["items"] = itemSchema,
                ["description"] = fieldDesc
            };
        }
        // 普通类型
        else
        {
            fieldSchema = GeneratePropertySchema(field.FieldType);
            if (!string.IsNullOrEmpty(fieldDesc))
                fieldSchema["description"] = fieldDesc;
        }
        
        properties[fieldName] = fieldSchema;
        
        if (isRequired)
            required.Add(fieldName);
    }
    
    if (required.Count == 0)
        schema.Remove("required");
    
    return schema;
}
```

**验证：**
- 简单自定义类型生成正确的Schema
- 嵌套自定义类型生成嵌套Schema
- 数组类型生成正确的array schema
- Required字段正确标记
- 循环引用不会导致无限递归

---

## 任务5：实现自定义类型参数解析 ⏱️ 1.5小时

**需求描述：**
在工具调用时正确解析自定义类型参数，使用 Newtonsoft.Json 进行反序列化。

**功能点：**
- [ ] 在 `RegisterToolFromMethod()` 方法中添加自定义类型解析分支
- [ ] 实现 `ParseCustomTypeArgument()` 方法
- [ ] 实现 `ParseCustomTypeArrayArgument()` 方法
- [ ] 支持嵌套类型的递归解析

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Server/McpServer.cs` (行388-430)

**实现要点：**
```csharp
// 在参数解析循环中添加 (RegisterToolFromMethod方法)
else if (IsCustomType(param.ParameterType))
{
    args[i] = ParseCustomTypeArgument(requestParams.Arguments, param);
}
else if (IsCustomTypeArray(param.ParameterType))
{
    args[i] = ParseCustomTypeArrayArgument(requestParams.Arguments, param);
}

// 解析方法实现
private object ParseCustomTypeArgument(JObject arguments, ParameterInfo param)
{
    if (arguments == null || !arguments.TryGetValue(param.Name, out var token))
    {
        return param.HasDefaultValue 
            ? param.DefaultValue 
            : (param.ParameterType.IsValueType ? Activator.CreateInstance(param.ParameterType) : null);
    }
    
    try
    {
        return token.ToObject(param.ParameterType);
    }
    catch (Exception ex)
    {
        _logger?.Error($"Failed to parse custom type argument '{param.Name}': {ex.Message}");
        throw new McpException(McpErrorCode.InvalidParams, 
            $"Invalid parameter '{param.Name}': {ex.Message}");
    }
}

private object ParseCustomTypeArrayArgument(JObject arguments, ParameterInfo param)
{
    if (arguments == null || !arguments.TryGetValue(param.Name, out var token))
    {
        return param.HasDefaultValue 
            ? param.DefaultValue 
            : null;
    }
    
    try
    {
        return token.ToObject(param.ParameterType);
    }
    catch (Exception ex)
    {
        _logger?.Error($"Failed to parse custom type array argument '{param.Name}': {ex.Message}");
        throw new McpException(McpErrorCode.InvalidParams, 
            $"Invalid parameter '{param.Name}': {ex.Message}");
    }
}
```

**验证：**
- 简单自定义类型正确解析
- 嵌套类型正确解析
- 数组和List正确解析
- 解析失败时返回正确的错误信息
- 可选字段不传值时使用默认值

---

## 任务6：实现工具注册时的合法性检查 ⏱️ 1小时

**需求描述：**
在注册工具时检查所有参数类型的合法性，非法工具跳过注册并记录日志。

**功能点：**
- [ ] 在 `RegisterToolsFromClass()` 方法中添加验证逻辑
- [ ] 在 `AddTool()` 方法中添加验证逻辑（针对反射注册）
- [ ] 非法工具跳过注册并记录警告日志
- [ ] 使用 `_logger` 记录验证失败原因

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Server/McpServer.cs` (RegisterToolsFromClass / AddTool方法)

**实现要点：**
```csharp
// 在 GenerateInputSchema 方法开始处添加验证
private JObject GenerateInputSchema(MethodInfo method)
{
    var parameters = method.GetParameters();
    if (parameters.Length == 0)
    {
        return JObject.Parse("{\"type\":\"object\"}");
    }

    var schema = new JObject
    {
        ["type"] = "object",
        ["properties"] = new JObject(),
        ["required"] = new JArray()
    };

    var properties = (JObject)schema["properties"];
    var required = (JArray)schema["required"];

    foreach (var param in parameters)
    {
        if (param.ParameterType == typeof(CancellationToken) || 
            param.ParameterType == typeof(CallToolRequestParams) ||
            param.ParameterType == typeof(JObject))
        {
            continue;
        }

        // 检查自定义类型是否合法
        if (IsCustomType(param.ParameterType))
        {
            var (isValid, error) = ValidateCustomType(param.ParameterType);
            if (!isValid)
            {
                throw new McpException(McpErrorCode.InvalidParams, 
                    $"Tool '{method.Name}' has invalid custom type parameter '{param.Name}': {error}");
            }
        }
        else if (IsCustomTypeArray(param.ParameterType))
        {
            var itemType = GetCustomArrayElementType(param.ParameterType);
            var (isValid, error) = ValidateCustomType(itemType);
            if (!isValid)
            {
                throw new McpException(McpErrorCode.InvalidParams, 
                    $"Tool '{method.Name}' has invalid custom type array parameter '{param.Name}': {error}");
            }
        }

        // ... 继续现有的schema生成逻辑
    }
    
    // ...
}
```

**验证流程：**
```
工具方法 -> GenerateInputSchema -> 遍历参数 -> 
  ├─ 自定义类型 -> ValidateCustomType -> 
  │   ├─ 合法 -> 继续生成Schema
  │   └─ 非法 -> 抛出McpException
  └─ 普通类型 -> 继续生成Schema
```

**验证：**
- 合法工具正常注册
- 非法工具抛出异常并被捕获
- 错误日志包含详细的失败原因
- tools/list 只包含合法工具

---

## 任务7：创建测试用例 - 复杂嵌套类型 ⏱️ 30分钟

**需求描述：**
在 Samples 目录创建复杂嵌套类型示例，验证自定义类型的各种场景。

**功能点：**
- [ ] 创建 `Assets/Scripts/MCPForUnity/Samples/CustomTypes/` 目录
- [ ] 创建基础类型 `AddressInfo.cs`
- [ ] 创建嵌套类型 `PersonInfo.cs` (包含 AddressInfo)
- [ ] 创建深层嵌套类型 `TeamInfo.cs` (包含 PersonInfo[])
- [ ] 创建数组测试类型 `SceneConfig.cs`
- [ ] 创建非法类型 `InvalidCustomType.cs` (用于测试验证)

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Samples/CustomTypes/*.cs` (新建)

**验证：**
- 所有类型编译通过
- 类型结构清晰，便于测试

---

## 任务8：创建测试工具函数 ⏱️ 30分钟

**需求描述：**
在 EditorToolsList_Custom.cs 中编写测试工具，验证自定义类型参数的各种功能。

**功能点：**
- [ ] 添加使用基础自定义类型的工具
- [ ] 添加使用嵌套自定义类型的工具
- [ ] 添加使用自定义类型数组的工具
- [ ] 添加使用 List<自定义类型> 的工具
- [ ] 添加一个非法的自定义类型工具用于测试验证

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Editor/Tools/EditorToolsList_Custom.cs`

**验证：**
- 5个测试工具编译通过
- 在Unity Editor中注册工具
- test_invalid_type 工具应该被过滤掉

---

## 任务9：修改编辑器窗口显示非法工具 ⏱️ 45分钟

**需求描述：**
在编辑器窗口中显示工具的合法性状态，非法工具用红色标注并显示失败原因。

**功能点：**
- [ ] 在 `Tool` 类中添加 `IsValid` 属性和 `ValidationError` 属性
- [ ] 在 `McpServer` 中存储工具验证状态
- [ ] 修改 `McpServerEditorWindow.cs` 的绘制逻辑
- [ ] 非法工具显示红色，并显示验证失败原因
- [ ] 合法工具显示默认样式

**涉及文件：**
- `Assets/Scripts/MCPForUnity/Core/Protocol/Types/Tool.cs`
- `Assets/Scripts/MCPForUnity/Server/McpServer.cs`
- `Assets/Scripts/MCPForUnity/Editor/Window/McpServerEditorWindow.cs`

**UI 效果：**
```
✓ test_address          合法工具 - 基础自定义类型
✓ test_person           合法工具 - 嵌套自定义类型  
✓ test_team             合法工具 - 数组类型
✓ test_person_list      合法工具 - List类型
✗ test_invalid_type     [红色] 非法工具
    Tool 'test_invalid_type' has invalid custom type parameter 'data': 
    No valid fields with [JsonProperty] and [McpArgument] attributes found
```

**验证：**
- 合法工具显示绿色勾号
- 非法工具显示红色叉号
- 错误信息清晰可见
- tools/list 接口只返回合法工具（IsValid=true）

---

## 任务10：编译验证和测试 ⏱️ 30分钟

**需求描述：**
确保所有代码编译通过，并在Unity中测试功能。

**功能点：**
- [ ] 运行 Unity 批处理模式编译
- [ ] 检查编译错误并修复
- [ ] 在 Unity Editor 中测试工具注册
- [ ] 测试合法工具正常显示
- [ ] 测试非法工具显示红色
- [ ] 测试自定义类型参数的解析
- [ ] 使用MCP客户端测试工具调用

**测试命令：**
```bash
/Applications/Unity/Hub/Editor/2021.3.45f1c2/Unity.app/Contents/MacOS/Unity \
  -batchmode -projectPath . -quit -logFile -
```

**预期结果：**
- ✅ 编译通过无错误
- ✅ 合法工具正常注册
- ✅ 非法工具被标记
- ✅ 自定义类型参数正确解析
- ✅ 嵌套类型正确工作
- ✅ 数组类型正确工作
- ✅ tools/list 只返回合法工具

---

## 任务11：代码审查和清理 ⏱️ 30分钟

**需求描述：**
确保代码符合项目规范，清理临时代码和注释。

**功能点：**
- [ ] 检查代码风格符合 AGENTS.md 规范
- [ ] 确保使用正确的命名约定（PascalCase, _underscorePrefix）
- [ ] 检查所有 async 方法使用 CancellationToken
- [ ] 确保错误处理使用 McpException
- [ ] 移除调试代码和注释
- [ ] 检查 Imports 顺序（System -> Third-party -> UnityEngine -> Project）
- [ ] 确保所有字段有正确的 XML 文档注释

---

## 任务12：提交代码 ⏱️ 15分钟

**需求描述：**
将完成的代码提交到 feature_20260301 分支。

**功能点：**
- [ ] 查看修改的文件列表
- [ ] 添加所有相关文件
- [ ] 编写清晰的提交信息
- [ ] 提交到 feature_20260301 分支

**提交信息格式：**
```
feat: Add custom type parameter support for MCP tools

- Extend McpArgumentAttribute to support class fields
- Add custom type detection with IsCustomType() method
- Implement recursive validation with ValidateCustomType()
- Generate nested object JSON Schema for custom types
- Support custom type arrays and List<T>
- Validate tool registration and mark invalid tools
- Display invalid tools in red with error messages in editor window
- Add test cases with AddressInfo, PersonInfo, TeamInfo
- Add invalid type example for validation testing
```

---

## 🎯 技术要点总结

### 自定义类型约束规则
1. ✅ **必须**同时使用 `[JsonProperty("name")]` 和 `[McpArgument(...)]`
2. ✅ **必须**至少有一个符合约束的字段
3. ✅ 嵌套的自定义子类型也**必须**符合规则
4. ✅ 不符合规则的工具**标记为非法**但仍显示在编辑器（红色）
5. ✅ tools/list 接口**只返回合法工具**

### 支持的类型形式
- ✅ 单个自定义类型：`PersonInfo person`
- ✅ 数组形式：`PersonInfo[] people`
- ✅ List形式：`List<PersonInfo> people`
- ✅ 嵌套结构：多层嵌套的自定义类型
- ✅ 可选字段：`Required = false`

### 关键实现位置
| 功能 | 文件 | 方法/位置 |
|------|------|----------|
| Attribute扩展 | `Attributes.cs` | 行54-67 |
| 类型检测 | `McpServer.cs` | 新增 `IsCustomType()` |
| 类型验证 | `McpServer.cs` | 新增 `ValidateCustomType()` |
| Schema生成 | `McpServer.cs` | 扩展 `GenerateInputSchema()` |
| 参数解析 | `McpServer.cs` | 扩展 `RegisterToolFromMethod()` |
| 工具状态 | `Tool.cs` | 新增属性 |
| 编辑器显示 | `McpServerEditorWindow.cs` | 修改绘制逻辑 |

---

**总预估时间：约 7-8 小时**

---

## 📝 执行顺序建议

1. **任务1-2**：基础准备（25分钟）
2. **任务3-5**：核心功能实现（4小时）
3. **任务6**：注册验证（1小时）
4. **任务7-8**：测试用例（1小时）
5. **任务9**：UI改进（45分钟）
6. **任务10-12**：验证与提交（1.5小时）
